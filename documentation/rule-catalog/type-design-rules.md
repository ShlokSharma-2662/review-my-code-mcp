# Type Design Rules

## Existing Rules
- Suggest record or record struct for immutable data carriers when class declarations are found
- Detect interface naming not following I-prefix convention

## Add New Rules
- Example: Detect mutable public fields and suggest encapsulation
